#include "main.h"

/**
 * _abs  - Check description
 * Description: It calculate abs of a number.
 * @n: input number
 * Return: abs of a number.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return ((-1 * n));
	}
}
