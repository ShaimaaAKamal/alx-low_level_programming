#include "main.h"

/**
 * print_numbers - A function that checks for uppercase character.
 * Return: Nothing
 */
void print_numbers(void)
{
	char i;

	for (i = '0'; i <= '9'; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
